# Default server configuration

server {
	listen 80 default_server;
	listen [::]:80 default_server;

	root /var/www/empty;

	server_name _;

    access_log /var/log/poolctl/default-access.log;
    error_log  /var/log/poolctl/default-error.log;

	location ~ .* {
		rewrite ^ /empty.html break;
	}
}
